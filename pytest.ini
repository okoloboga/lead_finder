[pytest]
testpaths = tests
asyncio_mode = auto
markers =
    unit: Fast isolated unit tests.
    integration: DB-backed and service-integration tests.
    handler: Bot handler tests.
    slow: Long-running tests.
