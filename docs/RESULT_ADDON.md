# Lead Finder — Результаты реализации ADDON.md

**Дата:** 2026-01-14

## Обзор

Успешно реализованы две доработки из ADDON.md:

1. **Безопасный парсинг с задержками** — защита от бана аккаунта
2. **Сохранение ссылок на сообщения** — для ответа в чате вместо личных сообщений

---

## 1. Безопасный парсинг с задержками

### Что реализовано

#### Конфигурация режимов безопасности (`config.py`)

```python
# Три режима безопасности
SAFETY_MODE = "normal"  # fast, normal, careful

# Задержки для каждого режима (в секундах)
SAFETY_DELAYS = {
    "fast": {
        "between_chats": (15, 30),
        "between_requests": (1, 2),
        ...
    },
    "normal": {
        "between_chats": (30, 60),
        "between_requests": (2, 3),
        ...
    },
    "careful": {
        "between_chats": (60, 120),
        "between_requests": (3, 5),
        ...
    }
}

# Лимиты сессии
MAX_CHATS_PER_RUN = 5
MAX_PARTICIPANTS_PER_CHAT = 500
MAX_CHANNELS_PER_RUN = 50
MAX_SESSION_DURATION_MINUTES = 40
```

#### FloodWaitError обработка (`members_parser.py`, `enrichment/telegram.py`)

- Автоматическое ожидание при получении FloodWait от Telegram
- Добавляется буфер +10 секунд к требуемому времени ожидания
- Максимум 2 retry, затем пауза парсинга
- Сохранение частично собранных данных при паузе

#### Прогресс-индикация (`main.py`)

```
==================================================
🔍 Lead Finder v2 — Запуск парсинга
==================================================
📊 Чатов для парсинга: 3
📝 Сообщений на чат: 500
🛡️  Режим безопасности: normal
⏱️  Примерное время: ~10-15 мин
==================================================

⏳ Парсинг чата 1/3: @marketplace_pro
   [█░░]
   Сообщений: 250/500 (50%)
   ✅ Найдено участников: 45

   ⏳ Пауза 45 сек перед следующим чатом...
```

### Использование

```bash
# С режимом безопасности по умолчанию (normal)
python main.py parse @chat1 @chat2

# С осторожным режимом (максимальные задержки)
python main.py parse @chat1 --safety-mode careful

# С быстрым режимом (минимальные задержки, выше риск)
python main.py parse @chat1 --safety-mode fast
```

---

## 2. Сохранение ссылок на сообщения

### Что реализовано

#### Метаданные сообщений (`members_parser.py`)

Каждое сообщение теперь сохраняется с полными данными:

```python
{
    "message_id": 4521,
    "text": "Задолбался терять заявки...",
    "date": "2026-01-12T14:30:00+00:00",
    "chat_username": "marketplace_pro",
    "chat_id": -1001234567890,
    "is_public": True,
    "link": "t.me/marketplace_pro/4521",
    "freshness": "hot",  # hot, warm, cold, stale
    "age_display": "2 дн. назад"
}
```

#### Генерация ссылок

- **Публичные чаты:** `t.me/{username}/{message_id}`
- **Приватные чаты:** `t.me/c/{chat_id}/{message_id}`

#### Категории свежести

| Категория | Давность | Индикатор |
|-----------|----------|-----------|
| hot | < 3 дней | 🔥 |
| warm | < 7 дней | — |
| cold | < 30 дней | ⚠️ |
| stale | > 30 дней | 🥶 |

#### Бонусы к скору (`qualifier.py`)

```python
FRESHNESS_SCORE_BONUS = {
    "has_pain_message": 2,      # Есть сообщение с болью
    "fresh_message": 1,         # Свежее сообщение < 3 дней
    "multiple_pain_messages": 1 # 3+ сообщений
}
```

#### Приоритизация лидов

Лиды автоматически сортируются:
1. Сначала с горячими сообщениями (🔥)
2. Затем по количеству сообщений в чате

#### Обновлённый формат карточки (`output.py`)

```markdown
## Лид #1 🔥 — Оценка: 8/10 (базовая: 5 + бонус за свежесть: 3)

**Контакт:** @VerevkinAV77
**Канал:** @ivan_wb (2,340 подписчиков)

### 💬 Сообщения из чатов
*💡 Совет: Ответьте на сообщение в чате — это безопаснее...*

💬 🔥 **[2 дн. назад]** "Задолбался терять заявки между Excel..."
   📍 @marketplace_pro • [Открыть →](https://t.me/marketplace_pro/4521)

### 🎯 Варианты связи
- [📱 Написать в личку](https://t.me/VerevkinAV77)
- [💬 Ответить в чате](https://t.me/marketplace_pro/4521) *(рекомендуется)*
```

---

## Изменённые файлы

| Файл | Изменения |
|------|-----------|
| `config.py` | Добавлены настройки безопасности, задержек, лимитов |
| `modules/members_parser.py` | Метаданные сообщений, задержки, FloodWait |
| `modules/qualifier.py` | Бонусы за свежесть, freshness_summary |
| `modules/output.py` | Форматирование со ссылками и индикаторами |
| `main.py` | Прогресс-индикация, --safety-mode, лимиты сессии |
| `modules/enrichment/telegram.py` | Унифицированные задержки |
| `.env.example` | Новые переменные окружения |

---

## Новые переменные окружения

```env
# Режим безопасности: fast, normal, careful
SAFETY_MODE=normal

# Лимиты сессии
MAX_CHATS_PER_RUN=5
MAX_PARTICIPANTS_PER_CHAT=500
MAX_CHANNELS_PER_RUN=50
MAX_SESSION_DURATION_MINUTES=40
```

---

## Примеры использования

### Стандартный запуск
```bash
python main.py parse @marketplace_pro @wb_sellers --messages-limit 500
```

### С максимальной осторожностью
```bash
python main.py parse @marketplace_pro --safety-mode careful --messages-limit 200
```

### Быстрый режим (для тестирования)
```bash
python main.py parse @small_chat --safety-mode fast --max-leads 5
```

---

## Что получает пользователь

1. **Защита от бана** — рандомизированные задержки, лимиты, обработка FloodWait
2. **Ссылки на сообщения** — можно ответить прямо в чате
3. **Приоритизация** — горячие лиды показываются первыми
4. **Бонусы к скору** — свежие сообщения повышают оценку
5. **Прогресс-индикация** — видно сколько осталось ждать
