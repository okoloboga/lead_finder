# LeadSense — Модуль сбора болей и генерации контента

## Обзор

### Цель
Расширение LeadSense Bot модулем, который собирает и кластеризует боли предпринимателей из Telegram-чатов в базу данных, а затем генерирует черновики постов для Telegram-канала на основе этих болей.

### Зачем
- Контент-план для канала строится на реальных болях из чатов, а не на фантазиях
- Каждый пост попадает в актуальный запрос аудитории
- Процесс создания поста: 1 час → 15 минут (бот делает черновик, ты редактируешь)
- Побочный эффект: та же база болей усиливает квалификацию лидов

### Связь с текущей системой
Модуль встраивается в существующий пайплайн LeadSense v2/v3. Парсинг сообщений уже реализован (`members_parser.py`), метаданные сообщений сохраняются (ADDON.md). Новый модуль **переиспользует** этот поток данных — не дублирует парсинг, а добавляет этап анализа и хранения болей.

---

## Архитектура

### Место в пайплайне

```
Существующий пайплайн LeadSense v2/v3
──────────────────────────────────────────────

[Seed-чаты] → [Парсинг участников + сообщений] → [Квалификация] → [Карточки лидов]
                        │
                        │ (новая ветка)
                        ▼
              ┌──────────────────────┐
              │  PAIN COLLECTOR      │
              │                      │
              │  1. Извлечение болей │
              │  2. Классификация    │
              │  3. Сохранение в БД  │
              │  4. Кластеризация    │
              └──────────┬───────────┘
                         │
                         ▼
              ┌──────────────────────┐
              │  CONTENT GENERATOR   │
              │                      │
              │  1. Выбор топ-болей  │
              │  2. Генерация поста  │
              │  3. Доставка в бот   │
              └──────────────────────┘
```

### Принцип работы

1. **При каждом запуске программы** (ручном или по расписанию) — параллельно с поиском лидов бот анализирует ВСЕ сообщения из чатов на предмет болей, не только сообщения потенциальных лидов
2. **Боли сохраняются в отдельную таблицу** с метаданными (чат, дата, категория, частотность)
3. **По запросу пользователя** — бот показывает топ болей и генерирует черновик поста

---

## Модуль 1: Pain Collector

### 1.1 Извлечение болей из сообщений

#### Входные данные
- Сообщения из чатов, которые уже парсит `members_parser.py`
- Сейчас парсер собирает сообщения для идентификации активных участников
- Новое: **все сообщения** передаются в Pain Collector, не только от потенциальных лидов

#### Логика извлечения

Сообщения обрабатываются батчами (20-30 сообщений за один LLM-запрос для экономии токенов).

**Промпт для LLM:**

```
Ты — аналитик болей предпринимателей.

Из следующих сообщений из Telegram-чата "{chat_name}" извлеки боли — проблемы, жалобы, трудности, с которыми сталкиваются участники.

Правила:
1. Боль — это конкретная проблема, не общее обсуждение
2. Игнорируй спам, рекламу, оффтоп
3. Одно сообщение может содержать несколько болей
4. Сообщение может не содержать болей — это нормально

Для каждой найденной боли верни JSON:
{
  "pains": [
    {
      "text": "краткое описание боли своими словами (1-2 предложения)",
      "original_quote": "цитата из сообщения, подтверждающая боль",
      "category": "одна из категорий (см. ниже)",
      "intensity": "low / medium / high",
      "business_type": "тип бизнеса автора если понятен, иначе null",
      "source_message_index": 0
    }
  ]
}

Категории болей:
- operations: рутина, ручной труд, потеря заявок, хаос в процессах
- sales: проблемы с продажами, конверсией, лидогенерацией
- communication: проблемы с клиентами, командой, подрядчиками
- analytics: отсутствие данных, отчётов, понимания метрик
- automation: потребность в автоматизации, интеграции систем
- marketplace: проблемы с WB/Ozon/маркетплейсами
- hiring: проблемы с наймом, увольнением, управлением
- finance: проблемы с деньгами, учётом, налогами
- legal: юридические проблемы
- other: не подходит ни одна категория

Сообщения:
{messages}
```

#### Привязка к источнику

Каждая извлечённая боль привязывается к:
- Оригинальному сообщению (message_id, chat_id, chat_username)
- Ссылке на сообщение (уже реализовано в ADDON.md)
- Дате сообщения
- Автору (user_id, username) — если доступен

### 1.2 Хранение в БД

#### Новые таблицы

**Таблица `pains`:**

| Поле | Тип | Описание |
|------|-----|----------|
| id | INTEGER PK | Автоинкремент |
| program_id | INTEGER FK | Из какой программы |
| text | TEXT | Описание боли (сгенерированное LLM) |
| original_quote | TEXT | Оригинальная цитата из чата |
| category | TEXT | Категория боли |
| intensity | TEXT | low / medium / high |
| business_type | TEXT | Тип бизнеса (nullable) |
| source_chat | TEXT | Username или ID чата |
| source_message_id | INTEGER | ID сообщения |
| source_message_link | TEXT | Ссылка на сообщение |
| source_user_id | INTEGER | ID автора (nullable) |
| source_username | TEXT | Username автора (nullable) |
| message_date | DATETIME | Дата оригинального сообщения |
| collected_at | DATETIME | Когда собрана |
| cluster_id | INTEGER FK | К какому кластеру отнесена (nullable) |
| used_in_post | BOOLEAN | Использовалась ли в посте |

**Таблица `pain_clusters`:**

| Поле | Тип | Описание |
|------|-----|----------|
| id | INTEGER PK | Автоинкремент |
| program_id | INTEGER FK | Из какой программы |
| name | TEXT | Название кластера ("Потеря заявок в мессенджерах") |
| category | TEXT | Основная категория |
| description | TEXT | Описание проблемы обобщённо |
| pain_count | INTEGER | Количество болей в кластере |
| avg_intensity | FLOAT | Средняя интенсивность |
| first_seen | DATETIME | Когда впервые встречена |
| last_seen | DATETIME | Когда встречена последний раз |
| trend | TEXT | growing / stable / declining |
| post_generated | BOOLEAN | Был ли по ней сгенерирован пост |

**Таблица `generated_posts`:**

| Поле | Тип | Описание |
|------|-----|----------|
| id | INTEGER PK | Автоинкремент |
| cluster_id | INTEGER FK | На основе какого кластера |
| post_type | TEXT | scenario / insight / breakdown |
| title | TEXT | Заголовок поста |
| body | TEXT | Текст черновика |
| status | TEXT | draft / edited / published / rejected |
| generated_at | DATETIME | Когда сгенерирован |
| published_at | DATETIME | Когда опубликован (nullable) |

### 1.3 Кластеризация болей

#### Когда происходит
После каждого запуска программы, когда новые боли сохранены в БД.

#### Логика

Кластеризация через LLM (не векторная — проще, надёжнее для малых объёмов):

1. Берём все новые боли, которые ещё не привязаны к кластеру (`cluster_id IS NULL`)
2. Берём существующие кластеры с описаниями
3. LLM решает: отнести к существующему кластеру или создать новый

**Промпт:**

```
У тебя есть существующие кластеры болей:
{existing_clusters}

И новые боли, которые нужно распределить:
{new_pains}

Задача:
1. Каждую новую боль отнеси к существующему кластеру ИЛИ создай новый
2. Кластер объединяет боли с одной сутью, даже если формулировки разные
3. Для новых кластеров придумай чёткое название и описание

Верни JSON:
{
  "assignments": [
    {"pain_id": 1, "cluster_id": 5},
    {"pain_id": 2, "cluster_id": "new", "new_cluster_name": "...", "new_cluster_description": "..."}
  ]
}
```

#### Обновление статистики кластеров

После кластеризации пересчитать для каждого затронутого кластера:
- `pain_count` — количество болей
- `avg_intensity` — средняя интенсивность
- `last_seen` — дата последней боли
- `trend` — растёт ли частотность (сравнение последних 7 дней с предыдущими 7)

---

## Модуль 2: Content Generator

### 2.1 Выбор темы для поста

#### Автоматический рейтинг кластеров

Кластеры ранжируются по формуле:

```
score = pain_count × 2 + freshness_bonus + intensity_bonus - already_posted_penalty

где:
  freshness_bonus = 3 если last_seen < 3 дней, 1 если < 7 дней, 0 иначе
  intensity_bonus = avg_intensity × 2 (high=3, medium=2, low=1)
  already_posted_penalty = 10 если post_generated = true
```

При запросе генерации контента бот показывает топ-5 кластеров по этому скору.

### 2.2 Генерация черновика поста

#### Типы постов

Три типа, как обсуждали:

**1. Сценарий** — "У вас такая проблема? Вот как она решается"
**2. Инсайт** — короткое наблюдение из практики
**3. Разбор** — анализ тренда на основе частотности болей

#### Промпт для генерации

```
Ты — эксперт по автоматизации бизнеса с помощью AI.
Пишешь пост для Telegram-канала.

Целевая аудитория: владельцы малого и среднего бизнеса, 
селлеры маркетплейсов, предприниматели.

Тип поста: {post_type}

Кластер боли: {cluster_name}
Описание: {cluster_description}
Количество упоминаний: {pain_count}
Примеры цитат из чатов (анонимизированные):
{sample_quotes}

Задача:
- Напиши черновик поста на 800-1500 символов
- Начни с проблемы, знакомой читателю (не с решения)
- Покажи что решение реальное и уже работает
- Не упоминай конкретные имена и ники из цитат
- Тон: экспертный, но не менторский. Как коллега, не как училка
- Не используй: "Привет, друзья", канцелярит, водянистые вступления
- Заканчивай мягким призывом: вопрос или предложение обсудить

Формат ответа:
{
  "title": "заголовок / первая строка поста",
  "body": "текст поста",
  "hashtags": ["#тег1", "#тег2"]
}
```

### 2.3 Анонимизация

Перед передачей цитат в промпт генерации:
- Удалить username'ы (`@username` → `[автор]`)
- Удалить имена, если можно идентифицировать
- Удалить названия конкретных компаний (опционально — можно оставлять крупные бренды типа WB, Ozon)
- Не передавать ссылки на профили

---

## Интерфейс в боте

### Новые кнопки в главном меню

```
🎯 LeadSense

┌─────────────────────────────────┐
│  [📋 Мои программы]             │
│  [➕ Создать программу]          │
│  [📊 Статистика]                │
│  [🔥 Боли и контент]    ← NEW  │
│  [⚙️ Настройки]                 │
└─────────────────────────────────┘
```

### Экран "Боли и контент"

```
🔥 Боли и контент

Собрано болей: 156
Кластеров: 23
Постов сгенерировано: 4

┌─────────────────────────────────┐
│  [📊 Топ болей]                 │
│  [✍️ Сгенерировать пост]        │
│  [📝 Мои черновики]             │
│  [🏠 Главное меню]              │
└─────────────────────────────────┘
```

### Топ болей

При нажатии "📊 Топ болей":

```
📊 Топ болей за последние 30 дней

1. 🔥 Потеря заявок между мессенджерами (×23)
   📈 Растёт · operations · high
   
2. 🔥 Ручная обработка отзывов на WB (×18)
   📈 Растёт · marketplace · high

3. Нет аналитики по продажам (×14)
   ➡️ Стабильно · analytics · medium

4. Хаос в коммуникации с поставщиками (×11)
   ➡️ Стабильно · communication · medium

5. Не могут найти нормального разработчика (×8)
   📉 Снижается · hiring · low

┌─────────────────────────────────┐
│  [1] [2] [3] [4] [5]           │
│  [◀️ Назад]                     │
└─────────────────────────────────┘
```

### Детали кластера

При выборе кластера:

```
🔥 Потеря заявок между мессенджерами

📊 Статистика:
• Упоминаний: 23
• Интенсивность: высокая
• Тренд: 📈 растёт
• Первое упоминание: 12 дней назад
• Последнее: вчера

💬 Примеры из чатов:
"Задолбался терять заявки между Excel и мессенджерами"
"Опять клиент написал в WhatsApp, а менеджер не увидел"
"Третий раз за неделю потеряли заказ"

┌─────────────────────────────────┐
│  [✍️ Сгенерировать пост]        │
│  [💬 Все цитаты (23)]           │
│  [◀️ К топ болей]               │
└─────────────────────────────────┘
```

### Генерация поста

При нажатии "✍️ Сгенерировать пост":

```
✍️ Генерация поста

Кластер: Потеря заявок между мессенджерами

Выбери тип поста:

┌─────────────────────────────────┐
│  [📋 Сценарий]                  │
│  "У вас проблема? Вот решение"  │
│                                 │
│  [💡 Инсайт]                    │
│  Короткое наблюдение            │
│                                 │
│  [📊 Разбор тренда]             │
│  Анализ на основе данных        │
├─────────────────────────────────┤
│  [◀️ Назад]                     │
└─────────────────────────────────┘
```

После выбора типа:

```
⏳ Генерирую черновик...
```

Затем:

```
✍️ Черновик поста

━━━━━━━━━━━━━━━━━━━━━━━━━━━

Каждую неделю в чатах селлеров вижу одну 
и ту же боль: заявки теряются между 
мессенджерами. WhatsApp, Telegram, 
почта, Excel — и менеджер физически 
не успевает за всем следить.

...

#автоматизация #бизнес #AI

━━━━━━━━━━━━━━━━━━━━━━━━━━━

┌─────────────────────────────────┐
│  [🔄 Перегенерировать]          │
│  [✅ Сохранить в черновики]      │
│  [❌ Отбросить]                 │
├─────────────────────────────────┤
│  [◀️ Назад]                     │
└─────────────────────────────────┘
```

### Мои черновики

```
📝 Мои черновики

1. ✍️ Потеря заявок между мессенджерами
   Сценарий · 2 часа назад
   
2. ✍️ Ручная обработка отзывов на WB
   Инсайт · вчера

3. ✅ Хаос в аналитике продаж
   Опубликован · 3 дня назад

┌─────────────────────────────────┐
│  [1] [2] [3]                    │
│  [◀️ Назад]                     │
└─────────────────────────────────┘
```

При выборе черновика — полный текст + кнопки:

```
┌─────────────────────────────────┐
│  [✏️ Отметить опубликованным]   │
│  [🔄 Перегенерировать]          │
│  [🗑 Удалить]                   │
│  [◀️ Назад]                     │
└─────────────────────────────────┘
```

---

## Настройки модуля

В общих настройках бота добавить:

```
⚙️ Настройки сбора болей

Сбор болей: [Вкл ✅] [Выкл]
Минимальная интенсивность: [Любая ✅] [Medium+] [High]
Автокластеризация: [Вкл ✅] [Выкл]

┌─────────────────────────────────┐
│  [✅ Сохранить]                 │
│  [◀️ Назад]                     │
└─────────────────────────────────┘
```

---

## Интеграция с существующим кодом

### Что меняется в `members_parser.py`

Минимальные изменения. Добавить передачу всех сообщений (не только от потенциальных лидов) в новый модуль:

```python
# После парсинга сообщений чата (существующий код)
messages = await parse_chat_messages(chat, limit=messages_limit)

# NEW: передать все сообщения в pain collector
if config.PAIN_COLLECTION_ENABLED:
    await pain_collector.process_messages(
        messages=messages,
        chat_username=chat.username,
        chat_id=chat.id,
        program_id=program_id
    )
```

### Что меняется в `main.py`

После завершения основного пайплайна — запуск кластеризации:

```python
# После квалификации лидов (существующий код)
qualified_leads = await qualifier.qualify(candidates)

# NEW: кластеризация новых болей
if config.PAIN_COLLECTION_ENABLED:
    await pain_clusterer.cluster_new_pains(program_id)
```

### Новые модули

```
leadsense/
├── modules/
│   ├── ... (существующие)
│   ├── pain_collector.py      # NEW: извлечение болей из сообщений
│   ├── pain_clusterer.py      # NEW: кластеризация болей
│   └── content_generator.py   # NEW: генерация постов
│
├── prompts/
│   ├── ... (существующие)
│   ├── pain_extraction.txt    # NEW: промпт извлечения болей
│   ├── pain_clustering.txt    # NEW: промпт кластеризации
│   └── content_generation.txt # NEW: промпт генерации постов
│
├── bot/
│   ├── handlers/
│   │   ├── ... (существующие)
│   │   └── pains_handler.py   # NEW: обработчики для UI болей
│   └── keyboards/
│       ├── ... (существующие)
│       └── pains_kb.py        # NEW: клавиатуры для UI болей
```

### Миграция БД

Добавить три таблицы (`pains`, `pain_clusters`, `generated_posts`) к существующей схеме. Обратно совместимо — существующие таблицы не затрагиваются.

---

## Оценка затрат

### LLM-токены (на один запуск программы)

| Операция | Input tokens | Output tokens | Запросов | Итого |
|----------|-------------|---------------|----------|-------|
| Извлечение болей (500 сообщений / 30 за батч) | ~1500/батч | ~500/батч | ~17 | ~$0.02 |
| Кластеризация | ~2000 | ~500 | 1 | ~$0.002 |
| Генерация поста | ~1500 | ~800 | 1 (по запросу) | ~$0.002 |

**Итого за запуск:** ~$0.02-0.03 дополнительно к текущим расходам на квалификацию.

### Хранение
- SQLite справится — объём данных небольшой (тысячи записей, не миллионы)
- Размер БД: ~1-5 МБ за месяц активного использования

---

## Чеклист реализации

### Этап 1: Pain Collector (ядро)
- [ ] Таблицы `pains`, `pain_clusters`, `generated_posts` в БД
- [ ] Модуль `pain_collector.py` — извлечение болей батчами
- [ ] Промпт для извлечения болей
- [ ] Интеграция в `members_parser.py` — передача сообщений
- [ ] Тест: запуск программы → боли сохраняются в БД

### Этап 2: Кластеризация
- [ ] Модуль `pain_clusterer.py`
- [ ] Промпт для кластеризации
- [ ] Автозапуск после каждого парсинга
- [ ] Расчёт трендов (growing/stable/declining)
- [ ] Тест: повторные запуски → боли группируются в кластеры

### Этап 3: Интерфейс в боте
- [ ] Кнопка "🔥 Боли и контент" в главном меню
- [ ] Экран топ болей с рейтингом
- [ ] Детали кластера с цитатами
- [ ] Настройки модуля

### Этап 4: Content Generator
- [ ] Модуль `content_generator.py`
- [ ] Промпты для трёх типов постов
- [ ] Анонимизация цитат перед генерацией
- [ ] Генерация через бот с выбором типа
- [ ] Сохранение черновиков
- [ ] Экран "Мои черновики"

### Этап 5: Полировка
- [ ] Формула рейтинга кластеров
- [ ] Индикация использованных болей (чтобы не повторяться)
- [ ] Статистика по болям в общей статистике бота
- [ ] Тест: полный цикл парсинг → боли → кластер → пост

---

## Ограничения и заметки

- **Качество извлечения болей зависит от промпта.** Потребуется 2-3 итерации промпта на реальных данных
- **Кластеризация через LLM не идеальна** — при >100 кластерах может терять контекст. Решение: периодически запускать "мерж" кластеров
- **Модуль не заменяет редактуру.** Черновик — это 60% готового поста. Твой голос, нюансы, демо — добавляешь руками
- **Анонимизация — must have.** Нельзя публиковать цитаты с юзернеймами из приватных чатов